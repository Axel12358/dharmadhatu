    # 8. Instagram con Playwright (sin login)
    try:
        print("\n📸 Scraping Instagram (sin login)...")
        eventos_instagram = scrape_instagram_publico("psytrance", max_posts=10)
        for e in eventos_instagram:
            eventos.append({
                'nombre': e['nombre'],
                'fuente': 'Instagram',
                'fecha': e.get('fecha', 'N/A'),
                'lugar': e.get('lugar', 'N/A'),
                'organizador': e.get('organizador', 'N/A'),
                'email': 'N/A'
            })
        print(f"✅ Instagram: {len(eventos_instagram)} eventos")
    except Exception as e:
        print(f"⚠️ Error en Instagram: {e}")
        # Generar eventos de prueba
        for i in range(10):
            eventos.append({
                'nombre': f"Evento Instagram {i+1}",
                'fuente': 'Instagram (prueba)',
                'fecha': 'N/A',
                'lugar': 'N/A',
                'organizador': 'N/A',
                'email': 'N/A'
            })
        print(f"✅ Instagram (prueba): 10 eventos")from scrapers.instagram.scraper_publico import scrape_instagram_publicofrom scrapers.instagram.scraper_publico import scrape_instagram_publico
